# Python deps pour le producer + dashboard + scripts (venv local)
# PyFlink est installé dans l'image Docker Flink (flink/requirements.txt)
psycopg2-binary==2.9.9
pymongo==4.6.0
redis==5.0.1
kafka-python==2.0.2
confluent-kafka==2.3.0
python-dotenv==1.0.0
streamlit==1.37.1  # 1.32.0 : page blanche après connexion (bug front « reading 'vertical' ») dès que la page grossit
pandas==2.1.4
plotly==5.18.0
snowflake-connector-python==3.6.0
pyarrow==14.0.2
faker==22.0.0

# Entraînement/inférence du modèle de scoring fraude (ml/)
xgboost==2.0.3
scikit-learn==1.4.0
joblib==1.3.2

# Tracking d'expériences + registre de modèles (ml/train_fraud_model.py)
mlflow==2.14.1
# mlflow 2.14.1 importe encore pkg_resources (retiré de setuptools >= 81) —
# pin explicite pour ne pas casser sur une image de base récente.
setuptools<81

# Détection de drift / suivi de performance en continu (ml/monitor.py)
evidently==0.4.25

# Notebook d'audit données/ML (notebooks/audit_data_ml.ipynb) — cf. make notebook
jupyter==1.0.0
ipykernel==6.29.4
nbformat==5.10.4
nbconvert==7.16.4
