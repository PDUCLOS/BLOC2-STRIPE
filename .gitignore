# === Secrets (JAMAIS sur git) ===
.env
.env.local
.env.*
!.env.example

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === Données runtime des conteneurs (Postgres, Mongo, Redis, Kafka, Debezium, Flink) ===
data/

# === Artefacts modèle ML — générés par make ml-train, pas versionnés (cf. docs/ML_INTEGRATION_STRATEGY.md) ===
ml/models/*.pkl
ml/models/*.meta.json

# === Logs ===
*.log
logs/

# === IDE / OS / Agents ===
.idea/
.idea_modules/
.vscode/
.claude/
.DS_Store
*.swp
*.swo

# === Fichiers temporaires Office ===
~$*

# === Fichiers temporaires draw.io ===
.$*.bkp
.$*.dtmp

# === Terraform (state et secrets jamais versionnés ; .terraform.lock.hcl OUI) ===
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
terraform.tfvars
crash.log

# Générateurs locaux des livrables de présentation (docx, drawio) — hors dépôt
presentation/generators/
